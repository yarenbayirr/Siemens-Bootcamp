var slugify = require('slugify')

var result = slugify('Bugün İstanbul için güneşli bir hava bekleniyor') 

console.log(result);

const sayHi = (name) => 'Merhaba ' + name;
console.log(sayHi('Yaren'));