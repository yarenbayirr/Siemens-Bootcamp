import { ExistProductNameValidator } from './exist-product-name-validator';

describe('ExistProductNameValidator', () => {
  it('should create an instance', () => {
    expect(new ExistProductNameValidator()).toBeTruthy();
  });
});
