import { PublishStartEndDateValidator } from './publish-start-end-date-validator';

describe('PublishStartEndDateValidator', () => {
  it('should create an instance', () => {
    expect(new PublishStartEndDateValidator()).toBeTruthy();
  });
});
