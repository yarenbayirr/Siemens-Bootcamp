import { IsEmailExistValidator } from './is-email-exist-validator';

describe('IsEmailExistValidator', () => {
  it('should create an instance', () => {
    expect(new IsEmailExistValidator()).toBeTruthy();
  });
});
